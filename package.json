{
    "name": "nba-py-react",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "heroku-postbuild": "cd client && npm install && npm run build",
        "start": "npm run build --prefix ./client && gunicorn app:app",
        "dev": "npm run dev --prefix ./client"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {}
}
